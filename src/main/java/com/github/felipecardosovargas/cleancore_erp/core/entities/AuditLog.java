package com.github.felipecardosovargas.cleancore_erp.core.entities;

public record AuditLog(
        //AuditLog
        //├── id (UUID)
        //├── companyId (FK)
        //├── userId (FK)
        //├── actionType
        //├── entityType
        //├── entityId
        //├── timestamp
        //├── metadata (JSON)
) {
}
